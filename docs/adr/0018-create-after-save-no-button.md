# Mapped team creates the Desk ticket after save

Saving a Helpdesk Ticket on a mapped team is enough — no “Send to Desk” button. The Desk create runs after save and must not block the form. In-save HTTP was rejected (Helpdesk UI waits on Zoho). A required button was rejected: the Team Map is the switch.

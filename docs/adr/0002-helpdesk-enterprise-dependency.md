# Helpdesk Ticket is the Odoo side of a Ticket

The module depends on Enterprise `helpdesk`. The Odoo record in a Ticket is a Helpdesk Ticket, not a custom model or `project.task`. Community-only buyers are out of scope. A standalone model plus an optional Helpdesk bridge was rejected so v1 is a Helpdesk feature, not a parallel ticket app.

# Backfill is lookback, with an entire-history option

Turning a Connection on pulls open tickets plus closed tickets in a configurable lookback (default 90 days). The Connection may instead request the entire Desk history. Open-only hides recent closed work; entire-as-default dies on large orgs.

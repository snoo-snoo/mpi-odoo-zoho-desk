# Bidirectional Ticket Sync

The Apps product contract is full bidirectional Ticket Sync (fields, comments, assignees, custom fields), not a Desk-as-SoR mirror. One-way pull and create-from-Odoo-only were rejected because either side must be a place work happens. Conflict rules and the Odoo Ticket record are still open.
